# See https://wiki.hyprland.org/Configuring/Monitors/
# List current monitors and resolutions possible: hyprctl monitors
# Format: monitor = [port], resolution, position, scale

# Straight 1x setup for low-resolution displays like 1080p
env = GDK_SCALE,1
monitor=,preferred,auto,3

# Roku TV - 4K @ 60Hz with 2x scale (effective 1920x1080 layout, sharper rendering)
# Bottom-aligned with the LG DualUp (TV physically sits lower)
monitor = HDMI-A-1, 3840x2160@60, 0x1224, 2

# LG DualUp - native 2560x2880 with 1.25 scale (effective 2048x2304)
monitor = DP-2, preferred, 1920x0, 1.25

# Good compromise for 27" or 32" 4K monitors (but fractional!)
# env = GDK_SCALE,1.75
# monitor=,preferred,auto,1.6

# Straight 1x setup for low-resolution displays like 1080p or 1440p
# Or for ultrawide monitors like 34" 3440x1440 or 49" 5120x1440
# env = GDK_SCALE,1
# monitor=,preferred,auto,1

# Portrait/rotated secondary monitor (transform: 1 = 90°, 3 = 270°)
# monitor = DP-2, preferred, auto, 1, transform, 1

# Example for Framework 13 w/ 6K XDR Apple display
# monitor = DP-5, 6016x3384@60, auto, 2
# monitor = eDP-1, 2880x1920@120, auto, 2

# Disable the second ghost monitor on an Apple 6K XDR over Thunderbolt
# monitor=DP-2,disable
